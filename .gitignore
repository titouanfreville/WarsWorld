# editor / IDE / OS stuff
.DS_Store
/.idea

# secrets
.env*
!.env.example
!.env.production.example

# Local database dumps written by the compose `backup` service. Never commit these — they contain
# every user row.
/backups

# generated files
/.next
/next-env.d.ts
/dist
/node_modules
# https://www.typescriptlang.org/tsconfig#tsBuildInfoFile
tsconfig.tsbuildinfo
prisma/migrations/20230510171142_amogus/migration.sql

.ai
.omc
.moonlight
.moonlight-local